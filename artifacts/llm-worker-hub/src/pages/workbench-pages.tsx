import { useMemo, useState, type FormEvent } from 'react';
import { Link, useParams } from 'wouter';
import { useQueryClient } from '@tanstack/react-query';
import {
  ArrowLeft,
  ArrowRight,
  BadgeDollarSign,
  BarChart3,
  Check,
  CheckCircle2,
  ChevronRight,
  Clock3,
  FileCheck2,
  FileText,
  HandCoins,
  Layers3,
  NotebookPen,
  Plus,
  Send,
  ShieldCheck,
  SlidersHorizontal,
  Target,
  UserRound,
  Users,
  WalletCards,
  X,
} from 'lucide-react';
import {
  getGetTaskQueryKey,
  getGetWorkerProfileQueryKey,
  getListReviewQueueQueryKey,
  getListSubmissionsQueryKey,
  getListTasksQueryKey,
  useClaimTask,
  useCreateTask,
  useGetTask,
  useGetWorkerDashboard,
  useGetWorkerProfile,
  useListReviewQueue,
  useListSubmissions,
  useListTasks,
  useReviewSubmission,
  useSubmitTask,
  useUpdateWorkerProfile,
} from '@workspace/api-client-react';
import { TaskDifficulty, type TaskInput, type ReviewItem, type Submission } from '@workspace/api-client-react';
import { AppShell, CheckLine, EmptyBlock, ErrorBlock, LoadingBlock, Logo, MiniIcon, PageTitle, PublicNav, StatCard, StatusPill, TaskCard } from '@/components/workbench';

const money = (n: number) => `$${n.toFixed(2)}`;
const dateLabel = (value?: string | null) => value ? new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric' }).format(new Date(value)) : 'No deadline';
const timeAgo = (value?: string) => value ? new Intl.RelativeTimeFormat('en', { numeric: 'auto' }).format(Math.max(-30, Math.round((new Date(value).getTime() - Date.now()) / 86400000)), 'day') : 'Recently';

export function LandingPage() {
  return <div className="min-h-[100dvh] overflow-hidden bg-background">
    <PublicNav />
    <section className="relative mx-auto grid max-w-7xl items-center gap-12 px-5 pb-20 pt-14 sm:px-8 lg:grid-cols-[1.05fr_.95fr] lg:pb-28 lg:pt-20">
      <div className="relative z-10 animate-rise"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-[hsl(var(--primary)/.24)] bg-[hsl(var(--primary)/.07)] px-3 py-1.5 text-xs font-bold text-[hsl(var(--primary))]"><span className="size-1.5 rounded-full bg-[hsl(var(--accent))]" />A clearer way to do meaningful AI work</div><h1 className="max-w-2xl text-5xl font-extrabold leading-[.98] tracking-[-.07em] sm:text-7xl">Your judgment is<br /><span className="text-[hsl(var(--primary))]">the signal.</span></h1><p className="mt-7 max-w-xl text-lg leading-8 text-muted-foreground">LLM Network matches careful people with the training tasks where their perspective matters. Clear briefs, fair pay, and work you can feel good about.</p><div className="mt-9 flex flex-wrap items-center gap-3"><Link href="/sign-up" data-testid="link-hero-join" className="rounded-xl bg-[hsl(var(--primary))] px-5 py-3.5 text-sm font-extrabold text-[hsl(var(--primary-foreground))] shadow-soft hover:-translate-y-1">Find your first task <ArrowRight className="ml-2 inline" size={16} /></Link><a href="#how-it-works" data-testid="link-hero-how" className="rounded-xl border border-border px-5 py-3.5 text-sm font-extrabold hover:bg-muted">See how it works</a></div><div className="mt-12 flex flex-wrap items-center gap-7 text-xs font-semibold text-muted-foreground"><span className="flex items-center gap-2"><ShieldCheck size={16} className="text-[hsl(var(--primary))]" />Human-reviewed briefs</span><span className="flex items-center gap-2"><HandCoins size={16} className="text-[hsl(var(--accent))]" />Transparent payouts</span></div></div>
      <div className="relative grid-signal rounded-[2rem] border border-border/70 bg-[hsl(var(--secondary)/.45)] p-5 sm:p-8"><div className="absolute -right-10 -top-12 size-36 rounded-full bg-[hsl(var(--accent)/.18)] blur-3xl" /><div className="relative rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-7"><div className="flex items-center justify-between border-b border-border pb-5"><div><div className="eyebrow text-[hsl(var(--primary))]">Today’s workbench</div><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Matched for Ari</h2></div><span className="grid size-9 place-items-center rounded-full bg-[hsl(var(--primary))] text-xs font-bold text-white">AR</span></div><div className="mt-5 rounded-xl border border-[hsl(var(--primary)/.2)] bg-[hsl(var(--primary)/.05)] p-4"><div className="flex items-start justify-between"><div><span className="eyebrow text-[hsl(var(--primary))]">High signal match</span><h3 className="mt-2 font-extrabold">Compare nuanced assistant replies</h3></div><span className="font-mono text-[hsl(var(--primary))]">$18.40</span></div><div className="mt-5 flex items-center justify-between text-xs text-muted-foreground"><span className="flex items-center gap-1"><Clock3 size={13} />35 min</span><span>Writing · Intermediate</span></div></div><div className="mt-3 grid grid-cols-2 gap-3"><div className="rounded-xl bg-muted p-4"><div className="eyebrow text-muted-foreground">This week</div><div className="mt-2 text-2xl font-extrabold">$146.80</div><div className="mt-1 text-xs text-muted-foreground">+12% from last week</div></div><div className="rounded-xl bg-[hsl(var(--accent)/.12)] p-4"><div className="eyebrow text-[hsl(var(--accent-foreground)/.7)]">Your focus</div><div className="mt-2 text-2xl font-extrabold">4.8<span className="text-sm font-medium"> / 5</span></div><div className="mt-1 text-xs text-muted-foreground">reviewer clarity score</div></div></div><div className="mt-5 flex items-center gap-2 text-xs font-bold text-[hsl(var(--primary))]"><span className="grid size-5 place-items-center rounded-full bg-[hsl(var(--primary))] text-white"><Check size={12} /></span>Profile is ready for matching</div></div></div>
    </section>
    <section id="how-it-works" className="border-y border-border bg-[hsl(var(--secondary)/.42)]"><div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 lg:py-20"><div className="grid gap-8 lg:grid-cols-[.75fr_1.25fr]"><div><div className="eyebrow text-[hsl(var(--primary))]">A better rhythm</div><h2 className="mt-3 max-w-sm text-3xl font-extrabold tracking-[-.055em]">Work that respects your attention.</h2></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><span className="font-mono text-sm text-[hsl(var(--accent))]">01</span><h3 className="mt-9 font-extrabold">Tell us how you think</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Set your skills, languages, and hours. We use context, not just checkboxes.</p></div><div className="rounded-2xl border border-border bg-card p-5"><span className="font-mono text-sm text-[hsl(var(--accent))]">02</span><h3 className="mt-9 font-extrabold">See your best matches</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Every brief includes the why: scope, time, difficulty, and exact payout.</p></div><div className="rounded-2xl border border-border bg-card p-5"><span className="font-mono text-sm text-[hsl(var(--accent))]">03</span><h3 className="mt-9 font-extrabold">Build a trusted record</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Feedback is direct and useful. Strong work opens better opportunities.</p></div></div></div></div></section>
    <section id="fairness" className="mx-auto grid max-w-7xl gap-12 px-5 py-20 sm:px-8 lg:grid-cols-[1fr_.8fr] lg:py-28"><div><div className="eyebrow text-[hsl(var(--primary))]">The signal standard</div><h2 className="mt-4 max-w-xl text-4xl font-extrabold tracking-[-.06em]">Good work should never feel like a guessing game.</h2><p className="mt-5 max-w-lg text-base leading-7 text-muted-foreground">We make the invisible parts visible: why a task fits, how long it should take, when you’ll be paid, and what reviewers are looking for.</p></div><div className="space-y-4"><div className="flex gap-4 border-b border-border pb-5"><MiniIcon icon={Target} /><div><h3 className="font-extrabold">Matched with care</h3><p className="mt-1 text-sm text-muted-foreground">Your profile shapes the feed, so your time goes toward work you can do well.</p></div></div><div className="flex gap-4 border-b border-border pb-5"><MiniIcon icon={BadgeDollarSign} /><div><h3 className="font-extrabold">Payouts in plain sight</h3><p className="mt-1 text-sm text-muted-foreground">No hidden rates. See the amount before you claim.</p></div></div><div className="flex gap-4"><MiniIcon icon={UserRound} /><div><h3 className="font-extrabold">Humans in the loop</h3><p className="mt-1 text-sm text-muted-foreground">A real reviewer helps maintain quality and keeps the system accountable.</p></div></div></div></section>
    <footer id="support" className="border-t border-border px-5 py-8 sm:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 text-sm text-muted-foreground sm:flex-row"><Logo /><span>Focused work for thoughtful contributors.</span></div></footer>
  </div>;
}

export function AuthPage({ mode }: { mode: 'sign-in' | 'sign-up' }) {
  const [submitted, setSubmitted] = useState(false);
  return <div className="grid min-h-[100dvh] lg:grid-cols-[.85fr_1.15fr]"><div className="hidden bg-[hsl(var(--sidebar))] p-10 text-white lg:flex lg:flex-col lg:justify-between"><Logo inverse /><div className="max-w-md"><div className="eyebrow text-[hsl(var(--accent))]">Contributor network</div><h1 className="mt-5 text-5xl font-extrabold leading-[1.03] tracking-[-.07em]">Make the model<br /><span className="text-[hsl(var(--accent))]">more human.</span></h1><p className="mt-6 leading-7 text-white/60">A serious workbench for people whose judgment makes language models more useful, accurate, and responsible.</p></div><div className="text-xs text-white/40">LLM Network · Built for careful work</div></div><div className="grid place-items-center bg-background p-5 sm:p-10"><div className="w-full max-w-[420px]"><div className="mb-10 lg:hidden"><Logo /></div><Link href="/" data-testid="link-auth-back" className="mb-10 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft size={15} /> Back to home</Link><div className="eyebrow text-[hsl(var(--primary))]">{mode === 'sign-in' ? 'Welcome back' : 'Join the network'}</div><h1 className="mt-3 text-3xl font-extrabold tracking-[-.055em]">{mode === 'sign-in' ? 'Pick up where you left off.' : 'Bring your perspective.'}</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">{mode === 'sign-in' ? 'Sign in to see your matched work and current progress.' : 'Create a contributor account and get matched to your first task.'}</p>{submitted ? <div className="mt-8 rounded-2xl border border-[hsl(var(--primary)/.3)] bg-[hsl(var(--primary)/.06)] p-5" data-testid="status-auth-success"><CheckCircle2 className="text-[hsl(var(--primary))]" /><h3 className="mt-3 font-extrabold">You’re on the right track.</h3><p className="mt-1 text-sm text-muted-foreground">Authentication is ready to connect. Continue into the worker portal while your account is being prepared.</p><Link href="/dashboard" className="mt-4 inline-block font-bold text-[hsl(var(--primary))]" data-testid="link-auth-dashboard">Open worker portal <ArrowRight className="ml-1 inline" size={15} /></Link></div> : <form className="mt-8 space-y-4" onSubmit={(e) => { e.preventDefault(); setSubmitted(true); }}><label className="block text-sm font-bold">Email address<input required type="email" placeholder="you@example.com" data-testid="input-auth-email" className="mt-2 w-full rounded-xl border border-input bg-card px-4 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-[hsl(var(--primary))] focus:ring-2 focus:ring-[hsl(var(--primary)/.14)]" /></label><label className="block text-sm font-bold">Password<input required type="password" placeholder="At least 8 characters" data-testid="input-auth-password" className="mt-2 w-full rounded-xl border border-input bg-card px-4 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-[hsl(var(--primary))] focus:ring-2 focus:ring-[hsl(var(--primary)/.14)]" /></label>{mode === 'sign-up' && <label className="block text-sm font-bold">Your name<input required placeholder="How should we call you?" data-testid="input-auth-name" className="mt-2 w-full rounded-xl border border-input bg-card px-4 py-3 text-sm outline-none placeholder:text-muted-foreground focus:border-[hsl(var(--primary))] focus:ring-2 focus:ring-[hsl(var(--primary)/.14)]" /></label>}<button type="submit" data-testid="button-auth-submit" className="mt-2 w-full rounded-xl bg-[hsl(var(--primary))] py-3.5 text-sm font-extrabold text-white hover:-translate-y-0.5">{mode === 'sign-in' ? 'Sign in to your workbench' : 'Create contributor account'} <ArrowRight className="ml-1 inline" size={15} /></button><div className="relative py-3 text-center text-xs text-muted-foreground before:absolute before:left-0 before:top-1/2 before:h-px before:w-[42%] before:bg-border after:absolute after:right-0 after:top-1/2 after:h-px after:w-[42%] after:bg-border">or continue with</div><button type="button" data-testid="button-auth-google" onClick={() => setSubmitted(true)} className="w-full rounded-xl border border-border py-3 text-sm font-bold hover:bg-muted">Continue with Google</button></form>}<p className="mt-8 text-sm text-muted-foreground">{mode === 'sign-in' ? 'New to the network?' : 'Already have an account?'} <Link href={mode === 'sign-in' ? '/sign-up' : '/sign-in'} className="font-bold text-[hsl(var(--primary))]" data-testid="link-auth-switch">{mode === 'sign-in' ? 'Create an account' : 'Sign in'}</Link></p></div></div></div>;
}

export function DashboardPage() {
  const dashboard = useGetWorkerDashboard();
  const profileQuery = useGetWorkerProfile({ query: { queryKey: getGetWorkerProfileQueryKey() } });
  if (dashboard.isLoading) return <AppShell><LoadingBlock lines={7} /></AppShell>;
  if (dashboard.isError) return <AppShell><ErrorBlock message="Your dashboard is temporarily unavailable." /></AppShell>;
  const data = dashboard.data;
  if (!data) return <AppShell><EmptyBlock title="Your workbench is quiet" description="Once your profile is ready, matched tasks will appear here." /></AppShell>;
  const profile = data.profile ?? profileQuery.data;
  return <AppShell><div className="animate-rise"><div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="eyebrow mb-3 text-[hsl(var(--primary))]">Tuesday · Your workbench</div><h1 className="text-3xl font-extrabold tracking-[-.055em] sm:text-[2.65rem]">Good morning.</h1><p className="mt-2 text-sm text-muted-foreground">A focused start: you have work that fits your signal.</p></div><Link href="/tasks" data-testid="link-dashboard-browse" className="rounded-xl bg-[hsl(var(--primary))] px-4 py-3 text-center text-sm font-extrabold text-white hover:-translate-y-0.5">Browse matched tasks <ArrowRight className="ml-1 inline" size={15} /></Link></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Available tasks" value={data.stats.availableTasks} detail="matched to your profile" icon={Layers3Icon} tint="teal" /><StatCard label="Completed tasks" value={data.stats.completedTasks} detail="all-time contribution" icon={CheckCircle2} tint="blue" /><StatCard label="Pending review" value={data.stats.pendingReview} detail="usually reviewed in 48h" icon={Clock3} tint="orange" /><StatCard label="Earned to date" value={money(data.stats.earned)} detail="approved payouts" icon={WalletCards} tint="ink" /></div><div className="mt-8 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><section className="rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-6"><div className="flex items-center justify-between"><div><div className="eyebrow text-[hsl(var(--primary))]">Good fit, right now</div><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Featured tasks</h2></div><Link href="/tasks" className="text-xs font-extrabold text-[hsl(var(--primary))]" data-testid="link-dashboard-all-tasks">View all <ChevronRight className="inline" size={14} /></Link></div><div className="mt-5 grid gap-3">{data.featuredTasks?.length ? data.featuredTasks.slice(0, 3).map((task) => <TaskCard key={task.id} task={task} />) : <EmptyBlock title="No matches yet" description="Your next matched task will land here." />}</div></section><section className="rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-6"><div className="eyebrow text-[hsl(var(--accent-foreground)/.7)]">Profile readiness</div><div className="mt-2 flex items-end justify-between"><h2 className="text-xl font-extrabold tracking-[-.04em]">Make matching sharper</h2><span className="font-mono text-lg font-medium text-[hsl(var(--primary))]">{profile?.onboardingProgress ?? 0}%</span></div><div className="mt-4 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-[hsl(var(--accent))]" style={{ width: `${profile?.onboardingProgress ?? 0}%` }} /></div><div className="mt-6 space-y-4"><CheckLine done={Boolean(profile?.name)}>Basic profile</CheckLine><CheckLine done={Boolean(profile?.skills?.length)}>Skills and expertise</CheckLine><CheckLine done={Boolean(profile?.languages?.length)}>Languages</CheckLine><CheckLine done={Boolean(profile?.weeklyHours)}>Weekly availability</CheckLine></div><Link href="/settings" data-testid="link-dashboard-settings" className="mt-7 block rounded-xl border border-border py-3 text-center text-sm font-bold hover:bg-muted">Complete profile <ArrowRight className="ml-1 inline" size={15} /></Link></section></div><section className="mt-6 rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-6"><div className="flex items-center justify-between"><div><div className="eyebrow text-muted-foreground">Recent activity</div><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Your latest submissions</h2></div><Link href="/submissions" className="text-xs font-extrabold text-[hsl(var(--primary))]" data-testid="link-dashboard-submissions">Open history <ChevronRight className="inline" size={14} /></Link></div>{data.recentSubmissions?.length ? <div className="mt-5 divide-y divide-border">{data.recentSubmissions.slice(0, 4).map((submission) => <SubmissionRow key={submission.id} item={submission} />)}</div> : <div className="mt-4"><EmptyBlock title="No submissions yet" description="Claim a task and your progress will show up here." /></div>}</section></div></AppShell>;
}

function Layers3Icon() { return <SlidersHorizontal size={17} />; }

function SubmissionRow({ item }: { item: Submission }) {
  return <div className="flex flex-col gap-3 py-4 sm:flex-row sm:items-center sm:justify-between" data-testid={`row-submission-${item.id}`}><div className="flex items-center gap-3"><MiniIcon icon={FileText} /><div><div className="text-sm font-bold">{item.taskTitle}</div><div className="mt-1 text-xs text-muted-foreground">{timeAgo(item.submittedAt)} · {money(item.payout)}</div></div></div><StatusPill status={item.status} /></div>;
}

export function TasksPage() {
  const [category, setCategory] = useState('');
  const [status, setStatus] = useState<'available' | 'claimed' | 'submitted' | 'all'>('available');
  const [search, setSearch] = useState('');
  const params = useMemo(() => ({ status, ...(category ? { category } : {}) }), [status, category]);
  const tasks = useListTasks(params, { query: { queryKey: getListTasksQueryKey(params) } });
  const claim = useClaimTask();
  const queryClient = useQueryClient();
  const visible = (tasks.data ?? []).filter((task) => task.title.toLowerCase().includes(search.toLowerCase()) || task.description.toLowerCase().includes(search.toLowerCase()));
  const claimTask = (id: string) => claim.mutate({ taskId: id }, { onSuccess: () => { void queryClient.invalidateQueries({ queryKey: getListTasksQueryKey(params) }); } });
  return <AppShell><PageTitle eyebrow="Task feed" title="Work that fits your signal." description="Browse the tasks matched to your skills, language, and available hours." action={<div className="flex items-center gap-2 rounded-xl border border-border bg-card px-3 py-2 text-xs font-bold text-muted-foreground"><span className="size-2 rounded-full bg-emerald-500" />Matching is on</div>} /><div className="mb-6 flex flex-col gap-3 rounded-2xl border border-border bg-card p-3 shadow-soft md:flex-row"><label className="relative flex-1"><span className="sr-only">Search tasks</span><SlidersHorizontal className="absolute left-3 top-3 text-muted-foreground" size={16} /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search by task or skill" data-testid="input-task-search" className="w-full rounded-lg bg-muted/70 py-2.5 pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-[hsl(var(--primary)/.18)]" /></label><select value={status} onChange={(e) => setStatus(e.target.value as typeof status)} data-testid="select-task-status" className="rounded-lg border border-border bg-background px-3 py-2 text-sm font-semibold outline-none"><option value="available">Available now</option><option value="claimed">My claimed tasks</option><option value="submitted">Submitted</option><option value="all">All task states</option></select><input value={category} onChange={(e) => setCategory(e.target.value)} placeholder="Category" data-testid="input-task-category" className="rounded-lg border border-border bg-background px-3 py-2 text-sm outline-none md:w-40" /><button onClick={() => { setCategory(''); setSearch(''); setStatus('available'); }} data-testid="button-clear-filters" className="rounded-lg px-3 py-2 text-xs font-bold text-muted-foreground hover:bg-muted">Clear</button></div>{claim.isError && <div className="mb-5 rounded-xl border border-rose-200 bg-rose-50 p-3 text-sm text-rose-800" data-testid="status-claim-error">That task could not be claimed. It may have just been picked up.</div>}{tasks.isLoading ? <LoadingBlock lines={6} /> : tasks.isError ? <ErrorBlock message="The matched task feed could not be reached." /> : visible.length ? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map((task) => <TaskCard key={task.id} task={task} onClaim={task.status === 'available' ? () => claimTask(task.id) : undefined} />)}</div> : <EmptyBlock title="No tasks match those filters" description="Try opening up your task state or clearing the search. The best match may be just around the corner." action={<button onClick={() => { setCategory(''); setSearch(''); setStatus('available'); }} className="rounded-lg bg-[hsl(var(--primary))] px-4 py-2 text-xs font-bold text-white" data-testid="button-empty-reset">Reset filters</button>} />}</AppShell>;
}

export function TaskDetailPage() {
  const { taskId = '' } = useParams<{ taskId: string }>();
  const [response, setResponse] = useState('');
  const [notes, setNotes] = useState('');
  const [submitted, setSubmitted] = useState(false);
  const taskQuery = useGetTask(taskId, { query: { enabled: Boolean(taskId), queryKey: getGetTaskQueryKey(taskId) } });
  const claim = useClaimTask();
  const submit = useSubmitTask();
  const queryClient = useQueryClient();
  if (taskQuery.isLoading) return <AppShell><LoadingBlock lines={8} /></AppShell>;
  if (taskQuery.isError || !taskQuery.data) return <AppShell><ErrorBlock message="We couldn't find this task. It may no longer be available." /></AppShell>;
  const task = taskQuery.data;
  const canSubmit = response.trim().length > 0;
  const doClaim = () => claim.mutate({ taskId }, { onSuccess: () => { void queryClient.invalidateQueries({ queryKey: getGetTaskQueryKey(taskId) }); void queryClient.invalidateQueries({ queryKey: getListTasksQueryKey({ status: 'available' }) }); } });
  const doSubmit = (e: FormEvent) => { e.preventDefault(); if (!canSubmit) return; submit.mutate({ taskId, data: { response, notes } }, { onSuccess: () => { setSubmitted(true); void queryClient.invalidateQueries({ queryKey: getGetTaskQueryKey(taskId) }); void queryClient.invalidateQueries({ queryKey: getListSubmissionsQueryKey() }); } }); };
  return <AppShell><div className="mb-6"><Link href="/tasks" className="inline-flex items-center gap-2 text-sm font-bold text-muted-foreground hover:text-foreground" data-testid="link-back-tasks"><ArrowLeft size={15} /> Back to task feed</Link></div><div className="grid gap-6 xl:grid-cols-[1fr_360px]"><article className="rounded-2xl border border-border bg-card p-6 shadow-soft sm:p-8"><div className="flex flex-wrap items-center gap-2"><span className="eyebrow text-[hsl(var(--primary))]">{task.category}</span><StatusPill status={task.status} /><span className="rounded-full bg-muted px-2.5 py-1 text-[11px] font-bold capitalize text-muted-foreground">{task.difficulty}</span></div><h1 className="mt-5 max-w-3xl text-3xl font-extrabold tracking-[-.055em] sm:text-4xl">{task.title}</h1><div className="mt-6 flex flex-wrap gap-x-6 gap-y-2 border-y border-border py-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><Clock3 size={16} />{task.estimatedMinutes} minutes</span><span className="flex items-center gap-2"><BadgeDollarSign size={16} />{money(task.payout)} payout</span><span className="flex items-center gap-2"><Users size={16} />{task.submissionsCount} contributors</span></div><div className="prose prose-sm mt-8 max-w-3xl text-foreground"><p className="whitespace-pre-wrap leading-8">{task.description}</p></div><div className="mt-8 rounded-xl bg-muted/70 p-5"><h3 className="font-extrabold">What makes a strong response</h3><ul className="mt-3 space-y-2 text-sm leading-6 text-muted-foreground"><li className="flex gap-2"><Check className="mt-1 shrink-0 text-[hsl(var(--primary))]" size={15} />Follow the brief closely and explain your reasoning where helpful.</li><li className="flex gap-2"><Check className="mt-1 shrink-0 text-[hsl(var(--primary))]" size={15} />Use specific examples rather than general impressions.</li><li className="flex gap-2"><Check className="mt-1 shrink-0 text-[hsl(var(--primary))]" size={15} />Flag ambiguity instead of guessing silently.</li></ul></div></article><aside className="h-fit space-y-4 xl:sticky xl:top-24">{submitted ? <div className="rounded-2xl border border-[hsl(var(--primary)/.25)] bg-[hsl(var(--primary)/.06)] p-6" data-testid="status-submit-success"><CheckCircle2 className="text-[hsl(var(--primary))]" size={26} /><h2 className="mt-4 text-xl font-extrabold">Work submitted.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Your response is with the review team. You can track its status in your submission history.</p><Link href="/submissions" className="mt-5 block rounded-xl bg-[hsl(var(--primary))] py-3 text-center text-sm font-bold text-white" data-testid="link-view-submissions">View submissions</Link></div> : task.status === 'available' ? <div className="rounded-2xl border border-border bg-card p-5 shadow-soft"><div className="eyebrow text-[hsl(var(--accent-foreground)/.8)]">Ready when you are</div><h2 className="mt-2 text-xl font-extrabold">Claim this task</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Claiming holds this work for you while you complete it. You’ll see the response form right here.</p><button onClick={doClaim} disabled={claim.isPending} data-testid="button-detail-claim" className="mt-5 w-full rounded-xl bg-[hsl(var(--primary))] py-3 text-sm font-extrabold text-white disabled:opacity-60">{claim.isPending ? 'Claiming…' : 'Claim task'} <ArrowRight className="ml-1 inline" size={15} /></button></div> : <form onSubmit={doSubmit} className="rounded-2xl border border-border bg-card p-5 shadow-soft" data-testid="form-submit-task"><div className="eyebrow text-[hsl(var(--primary))]">Your response</div><h2 className="mt-2 text-xl font-extrabold">Send thoughtful work.</h2><label className="mt-5 block text-sm font-bold">Response<textarea required value={response} onChange={(e) => setResponse(e.target.value)} rows={8} placeholder="Write your response here…" data-testid="textarea-task-response" className="mt-2 w-full resize-y rounded-xl border border-input bg-background px-3 py-3 text-sm leading-6 outline-none focus:border-[hsl(var(--primary))]" /></label><label className="mt-4 block text-sm font-bold">Notes <span className="font-normal text-muted-foreground">(optional)</span><textarea value={notes} onChange={(e) => setNotes(e.target.value)} rows={3} placeholder="Anything the reviewer should know?" data-testid="textarea-task-notes" className="mt-2 w-full resize-y rounded-xl border border-input bg-background px-3 py-3 text-sm leading-6 outline-none focus:border-[hsl(var(--primary))]" /></label>{submit.isError && <p className="mt-3 text-xs font-semibold text-rose-700" data-testid="status-submit-error">Submission failed. Please try again.</p>}<button disabled={!canSubmit || submit.isPending} data-testid="button-submit-task" className="mt-5 w-full rounded-xl bg-[hsl(var(--primary))] py-3 text-sm font-extrabold text-white disabled:opacity-50">{submit.isPending ? 'Sending…' : 'Submit for review'} <Send className="ml-1 inline" size={14} /></button></form>}</aside></div></AppShell>;
}

export function SubmissionsPage() {
  const submissions = useListSubmissions();
  const [filter, setFilter] = useState('all');
  const visible = (submissions.data ?? []).filter((item) => filter === 'all' || item.status === filter);
  return <AppShell><PageTitle eyebrow="Submission history" title="A record of your work." description="Track every response from first submission to final review." action={<select value={filter} onChange={(e) => setFilter(e.target.value)} data-testid="select-submission-filter" className="rounded-xl border border-border bg-card px-3 py-2.5 text-sm font-bold"><option value="all">All statuses</option><option value="submitted">In review</option><option value="approved">Approved</option><option value="rejected">Needs another pass</option></select>} />{submissions.isLoading ? <LoadingBlock lines={6} /> : submissions.isError ? <ErrorBlock message="Submission history could not be loaded." /> : visible.length ? <div className="overflow-hidden rounded-2xl border border-border bg-card shadow-soft"><div className="hidden grid-cols-[1.4fr_.65fr_.65fr_.7fr] gap-4 border-b border-border bg-muted/50 px-5 py-3 text-[11px] font-bold uppercase tracking-wider text-muted-foreground md:grid"><span>Task</span><span>Submitted</span><span>Payout</span><span>Status</span></div>{visible.map((item) => <div key={item.id} className="grid gap-3 border-b border-border px-5 py-5 last:border-0 md:grid-cols-[1.4fr_.65fr_.65fr_.7fr] md:items-center md:gap-4" data-testid={`row-history-${item.id}`}><div><div className="font-bold">{item.taskTitle}</div><div className="mt-1 text-xs text-muted-foreground">{item.reviewerNote ?? 'No reviewer note yet'}</div></div><div className="text-sm text-muted-foreground"><span className="mr-2 text-[10px] font-bold uppercase text-muted-foreground md:hidden">Submitted</span>{dateLabel(item.submittedAt)}</div><div className="font-mono text-sm">{money(item.payout)}</div><div><StatusPill status={item.status} /></div></div>)}</div> : <EmptyBlock title="No submissions in this view" description="Once you send work for review, you’ll have a clear timeline here." action={<Link href="/tasks" className="rounded-lg bg-[hsl(var(--primary))] px-4 py-2 text-xs font-bold text-white" data-testid="link-empty-tasks">Browse tasks</Link>} />}</AppShell>;
}

export function SettingsPage() {
  const profileQuery = useGetWorkerProfile();
  const update = useUpdateWorkerProfile();
  const queryClient = useQueryClient();
  const profile = profileQuery.data;
  const [availability, setAvailability] = useState<'available' | 'away'>('available');
  const [hours, setHours] = useState('20');
  const [experience, setExperience] = useState('');
  const [saved, setSaved] = useState(false);
  if (profileQuery.isLoading) return <AppShell><LoadingBlock lines={7} /></AppShell>;
  if (profileQuery.isError || !profile) return <AppShell><ErrorBlock message="Profile settings are unavailable." /></AppShell>;
  const save = (e: FormEvent) => { e.preventDefault(); update.mutate({ data: { availability, weeklyHours: Number(hours), experience } }, { onSuccess: () => { setSaved(true); void queryClient.invalidateQueries({ queryKey: getGetWorkerProfileQueryKey() }); setTimeout(() => setSaved(false), 3000); } }); };
  return <AppShell><PageTitle eyebrow="Settings" title="Shape your workbench." description="Keep your profile honest so the right opportunities find you." /><form onSubmit={save} className="grid gap-6 xl:grid-cols-[1fr_340px]"><div className="space-y-6"><section className="rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-7"><div className="flex items-start justify-between"><div><h2 className="text-xl font-extrabold tracking-[-.04em]">Availability</h2><p className="mt-1 text-sm text-muted-foreground">Let matching know when you can take on work.</p></div><MiniIcon icon={Clock3} /></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => setAvailability('available')} data-testid="button-availability-available" className={`rounded-xl border p-4 text-left ${availability === 'available' ? 'border-[hsl(var(--primary))] bg-[hsl(var(--primary)/.06)]' : 'border-border hover:bg-muted'}`}><div className="flex items-center justify-between"><span className="font-bold">Available for work</span><span className={`size-3 rounded-full ${availability === 'available' ? 'bg-emerald-500' : 'bg-muted'}`} /></div><p className="mt-1 text-xs text-muted-foreground">Show me in active matching.</p></button><button type="button" onClick={() => setAvailability('away')} data-testid="button-availability-away" className={`rounded-xl border p-4 text-left ${availability === 'away' ? 'border-[hsl(var(--accent))] bg-[hsl(var(--accent)/.08)]' : 'border-border hover:bg-muted'}`}><div className="flex items-center justify-between"><span className="font-bold">Take a pause</span><span className={`size-3 rounded-full ${availability === 'away' ? 'bg-[hsl(var(--accent))]' : 'bg-muted'}`} /></div><p className="mt-1 text-xs text-muted-foreground">Keep my profile, pause new tasks.</p></button></div><label className="mt-5 block text-sm font-bold">Weekly hours <span className="font-normal text-muted-foreground">({hours} hours)</span><input type="range" min="1" max="40" value={hours} onChange={(e) => setHours(e.target.value)} data-testid="input-weekly-hours" className="mt-4 w-full accent-[hsl(var(--primary))]" /><span className="mt-1 flex justify-between text-[11px] text-muted-foreground"><span>1 hour</span><span>40 hours</span></span></label></section><section className="rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-7"><div className="flex items-start justify-between"><div><h2 className="text-xl font-extrabold tracking-[-.04em]">Profile context</h2><p className="mt-1 text-sm text-muted-foreground">A little more context helps us match with nuance.</p></div><MiniIcon icon={NotebookPen} /></div><label className="mt-6 block text-sm font-bold">Experience<textarea value={experience || profile.experience || ''} onChange={(e) => setExperience(e.target.value)} rows={5} placeholder="Tell us about the kinds of language, research, or domain work you do well." data-testid="textarea-profile-experience" className="mt-2 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm leading-6 outline-none focus:border-[hsl(var(--primary))]" /></label><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><div className="mb-2 text-sm font-bold">Skills</div><div className="flex flex-wrap gap-2">{profile.skills.map((skill) => <span key={skill} className="rounded-lg bg-muted px-2.5 py-1.5 text-xs font-bold">{skill}</span>)}</div></div><div><div className="mb-2 text-sm font-bold">Languages</div><div className="flex flex-wrap gap-2">{profile.languages.map((language) => <span key={language} className="rounded-lg bg-muted px-2.5 py-1.5 text-xs font-bold">{language}</span>)}</div></div></div></section><div className="flex flex-col gap-3 sm:flex-row sm:items-center"><button type="submit" disabled={update.isPending} data-testid="button-save-settings" className="rounded-xl bg-[hsl(var(--primary))] px-5 py-3 text-sm font-extrabold text-white disabled:opacity-50">{update.isPending ? 'Saving…' : 'Save changes'} <Check className="ml-1 inline" size={15} /></button>{saved && <span className="text-sm font-bold text-emerald-700" data-testid="status-settings-saved">Changes saved.</span>}{update.isError && <span className="text-sm font-bold text-rose-700" data-testid="status-settings-error">Could not save. Try again.</span>}</div></div><aside className="h-fit rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-7 xl:sticky xl:top-24"><div className="eyebrow text-[hsl(var(--primary))]">Profile snapshot</div><div className="mt-4 flex items-center gap-3"><span className="grid size-12 place-items-center rounded-full bg-[hsl(var(--primary))] font-bold text-white">{profile.avatarInitials ?? 'AR'}</span><div><div className="font-extrabold">{profile.name}</div><div className="text-xs text-muted-foreground">{profile.email}</div></div></div><div className="mt-6 space-y-4 border-t border-border pt-5 text-sm"><div className="flex items-center justify-between"><span className="text-muted-foreground">Status</span><StatusPill status={profile.status} /></div><div className="flex items-center justify-between"><span className="text-muted-foreground">Timezone</span><span className="font-semibold">{profile.timezone ?? 'Not set'}</span></div><div className="flex items-center justify-between"><span className="text-muted-foreground">Profile strength</span><span className="font-mono font-medium text-[hsl(var(--primary))]">{profile.onboardingProgress}%</span></div></div><div className="mt-6 rounded-xl bg-[hsl(var(--secondary)/.6)] p-4 text-xs leading-5 text-muted-foreground"><ShieldCheck className="mb-2 text-[hsl(var(--primary))]" size={17} />Only the information needed to match you with fair, relevant work is shared with task operators.</div></aside></form></AppShell>;
}

export function AdminPage() {
  const queue = useListReviewQueue();
  const create = useCreateTask();
  const review = useReviewSubmission();
  const queryClient = useQueryClient();
  const [showCreate, setShowCreate] = useState(false);
  const [reviewNote, setReviewNote] = useState<Record<string, string>>({});
  const [form, setForm] = useState<TaskInput>({ title: '', description: '', category: 'Writing', difficulty: TaskDifficulty.intermediate, estimatedMinutes: 30, payout: 12, requiredSkills: ['English'] });
  const publish = (e: FormEvent) => { e.preventDefault(); create.mutate({ data: form }, { onSuccess: () => { setShowCreate(false); setForm({ title: '', description: '', category: 'Writing', difficulty: TaskDifficulty.intermediate, estimatedMinutes: 30, payout: 12, requiredSkills: ['English'] }); } }); };
  const decide = (item: ReviewItem, status: 'approved' | 'rejected') => review.mutate({ submissionId: item.id, data: { status, reviewerNote: reviewNote[item.id] } }, { onSuccess: () => { void queryClient.invalidateQueries({ queryKey: getListReviewQueueQueryKey() }); } });
  return <AppShell admin><PageTitle eyebrow="Operator desk" title="Keep the signal clean." description="Publish clear work, review with consistency, and keep contributors moving." action={<button onClick={() => setShowCreate(!showCreate)} data-testid="button-toggle-create-task" className="rounded-xl bg-[hsl(var(--primary))] px-4 py-3 text-sm font-extrabold text-white"><Plus className="mr-1 inline" size={15} /> Publish task</button>} />{showCreate && <form onSubmit={publish} className="mb-6 rounded-2xl border border-[hsl(var(--primary)/.28)] bg-[hsl(var(--primary)/.04)] p-5 shadow-soft sm:p-7" data-testid="form-create-task"><div className="mb-5 flex items-center justify-between"><div><div className="eyebrow text-[hsl(var(--primary))]">New task</div><h2 className="mt-1 text-xl font-extrabold">Give contributors a brief worth trusting.</h2></div><button type="button" onClick={() => setShowCreate(false)} data-testid="button-close-create-task" className="rounded-lg p-2 hover:bg-muted"><X size={18} /></button></div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold sm:col-span-2">Title<input required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} data-testid="input-create-title" className="mt-2 w-full rounded-xl border border-input bg-card px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-bold sm:col-span-2">Description<textarea required value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} rows={4} data-testid="textarea-create-description" className="mt-2 w-full rounded-xl border border-input bg-card px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-bold">Category<input required value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} data-testid="input-create-category" className="mt-2 w-full rounded-xl border border-input bg-card px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-bold">Difficulty<select value={form.difficulty} onChange={(e) => setForm({ ...form, difficulty: e.target.value as TaskInput['difficulty'] })} data-testid="select-create-difficulty" className="mt-2 w-full rounded-xl border border-input bg-card px-3 py-2.5 text-sm outline-none"><option value="starter">Starter</option><option value="intermediate">Intermediate</option><option value="advanced">Advanced</option></select></label><label className="text-sm font-bold">Estimated minutes<input type="number" min="1" value={form.estimatedMinutes} onChange={(e) => setForm({ ...form, estimatedMinutes: Number(e.target.value) })} data-testid="input-create-minutes" className="mt-2 w-full rounded-xl border border-input bg-card px-3 py-2.5 text-sm outline-none" /></label><label className="text-sm font-bold">Payout<input type="number" min="0" step=".01" value={form.payout} onChange={(e) => setForm({ ...form, payout: Number(e.target.value) })} data-testid="input-create-payout" className="mt-2 w-full rounded-xl border border-input bg-card px-3 py-2.5 text-sm outline-none" /></label></div><button disabled={create.isPending} data-testid="button-publish-task" className="mt-5 rounded-xl bg-[hsl(var(--primary))] px-5 py-3 text-sm font-extrabold text-white disabled:opacity-60">{create.isPending ? 'Publishing…' : 'Publish task'} <ArrowRight className="ml-1 inline" size={15} /></button>{create.isError && <span className="ml-3 text-sm font-semibold text-rose-700">Could not publish this task.</span>}</form>}<div className="grid gap-4 sm:grid-cols-3"><StatCard label="Awaiting review" value={queue.data?.length ?? 0} detail="responses need your eye" icon={FileCheck} tint="orange" /><StatCard label="Live tasks" value="—" detail="available in the feed" icon={Layers3Icon} tint="teal" /><StatCard label="Quality signal" value="4.8" detail="average worker clarity" icon={BarChart3} tint="blue" /></div><section className="mt-6 rounded-2xl border border-border bg-card p-5 shadow-soft sm:p-7"><div className="flex items-center justify-between"><div><div className="eyebrow text-[hsl(var(--accent-foreground)/.8)]">Needs your judgment</div><h2 className="mt-2 text-xl font-extrabold tracking-[-.04em]">Review queue</h2></div><span className="rounded-full bg-[hsl(var(--accent)/.15)] px-2.5 py-1 text-xs font-bold text-[hsl(var(--accent-foreground))]">{queue.data?.length ?? 0} waiting</span></div>{queue.isLoading ? <div className="mt-5"><LoadingBlock lines={4} /></div> : queue.isError ? <div className="mt-5"><ErrorBlock message="The review queue could not be reached." /></div> : queue.data?.length ? <div className="mt-5 grid gap-4">{queue.data.map((item) => <div key={item.id} className="rounded-xl border border-border p-5" data-testid={`review-item-${item.id}`}><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><div className="flex flex-wrap items-center gap-2"><span className="eyebrow text-[hsl(var(--primary))]">{item.category}</span><span className="text-xs text-muted-foreground">{dateLabel(item.submittedAt)}</span></div><h3 className="mt-2 font-extrabold">{item.taskTitle}</h3><p className="mt-1 text-xs text-muted-foreground">{item.workerName ?? 'Contributor'} · {item.workerEmail}</p></div><span className="font-mono text-[hsl(var(--primary))]">{money(item.payout)}</span></div><div className="mt-4 rounded-xl bg-muted/60 p-4 text-sm leading-6">{item.response ?? 'No response text provided.'}</div><textarea value={reviewNote[item.id] ?? ''} onChange={(e) => setReviewNote({ ...reviewNote, [item.id]: e.target.value })} rows={2} placeholder="Optional note for the contributor" data-testid={`textarea-review-note-${item.id}`} className="mt-4 w-full rounded-xl border border-input bg-background px-3 py-2.5 text-sm outline-none" /><div className="mt-3 flex flex-wrap justify-end gap-2"><button onClick={() => decide(item, 'rejected')} disabled={review.isPending} data-testid={`button-reject-${item.id}`} className="rounded-lg border border-rose-200 px-3 py-2 text-xs font-bold text-rose-700 hover:bg-rose-50">Needs another pass</button><button onClick={() => decide(item, 'approved')} disabled={review.isPending} data-testid={`button-approve-${item.id}`} className="rounded-lg bg-[hsl(var(--primary))] px-3 py-2 text-xs font-bold text-white hover:-translate-y-0.5">Approve payout <Check size={13} className="ml-1 inline" /></button></div></div>)}</div> : <div className="mt-5"><EmptyBlock title="Queue is clear" description="No submissions are waiting for review. Nice work." /></div>}</section></AppShell>;
}

function FileCheck() { return <FileText size={17} />; }